第11章 持有对象

    11.6 迭代器
        1. 迭代器是一种轻量级对象，使用迭代器会有一些奇怪限制：
            1) Java的Iterator只能单向用移动。
            2) 使用iterator()方法要求容器返回一个Iterator。
            3) 使用next()获得序列中的下一个元素。
            4) 使用hasNext()检查系列中是否还有元素。
            5) 使用remove()将迭代器新近返回的元素删除。